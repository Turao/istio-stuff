bases:
- github.com/argoproj/argo-cd//manifests/cluster-install?ref=v1.7.5

# additional resources like ingress rules, cluster and repository secrets.
# resources:
# - clusters-secrets.yaml
# - repos-secrets.yaml

# changes to config maps
patchesStrategicMerge:
- config.yaml